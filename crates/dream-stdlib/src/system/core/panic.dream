module system.core;

// Halt used by bootstrap (`Option.unwrap`, bounds checks). Full console I/O lives on `extend System`
// in `system.dream` (`import system;`).
public static class System {
    @intrinsic("dream_panic")
    static extern fun panic(borrow message: string): void;
}
