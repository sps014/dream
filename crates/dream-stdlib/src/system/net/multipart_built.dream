module system.net;

// Result of `MultipartForm.build`.

public struct MultipartBuilt {
    // Headers including `Content-Type` with boundary.
    public headers: HttpHeaders;
    // Encoded multipart body.
    public body: byte[];

    // Packs the Content-Type headers and encoded body from `MultipartForm.build`.
    internal constructor(headers: HttpHeaders, body: byte[]) {
        this.headers = headers;
        this.body = body;
    }
}
