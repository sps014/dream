import system;
import gen;

@dto
class Point {
    public x: int;
    public y: int;

    public constructor(x: int, y: int) {
        this.x = x;
        this.y = y;
    }
}

fun main(): void {
    let p = Point(1, 2);
    System.println(p.describe());
}
