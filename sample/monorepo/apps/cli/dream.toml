[package]
name = "cli"
version = "0.1.0"
type = "bin"
entry = "src/main.dream"
targets = ["native"]
description = "CLI app that depends on the greeter workspace member"

[dependencies]
# version + path: local develop via path; publishable with the version req
greeter = { version = "0.1.0", path = "../../packages/greeter" }
