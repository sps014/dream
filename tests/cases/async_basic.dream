import system;
async fun delayedDouble(n: int): int {
    Time.sleep(100).await;
    return n * 2;
}

async fun main(): void {
    System.println("start");
    let a = delayedDouble(10).await;
    let b = delayedDouble(a).await;
    System.println("result = " + b);
    System.println("done");
}
