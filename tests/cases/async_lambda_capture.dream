import system;
fun make_async_adder(n: int): fun(int): Future<int> {
    return async (x) => {
        Time.sleep(1).await;
        return x + n;
    };
}

async fun main(): void {
    let add5: fun(int): Future<int> = make_async_adder(5);
    System.println(add5(10).await);
    System.println(add5(20).await);
}
