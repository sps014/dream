import system;
async fun main(): void {
    // Async lambdas are `fun(...): Future<T>` values — a plain `fun(...): T` context is wrong.
    let f: fun(int): int = async (x) => x * 2;
    System.println(f(21).await);
}
