// `await` nested inside a map-literal value in a non-async function was previously missed by the
// await-placement expression walk.
import system;
async fun work(): int { return 1; }
fun f() {
    let m: Map<string, int> = { "k": await work() };
    System.println(m["k"]);
}
fun main() { }
