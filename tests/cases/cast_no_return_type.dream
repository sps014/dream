// `fun implicit` requires a declared return type (the cast's target type).
import system;
class Wrapper {
    public value: int;

    public constructor(value: int) {
        this.value = value;
    }

        fun implicit() {
        this.value = this.value;
    }
}

fun main() {
    let w = Wrapper(1);
    System.println(w.value);
}
