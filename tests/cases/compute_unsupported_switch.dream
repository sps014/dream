// Pattern-matching switch is not lowered to WGSL.
import system.gpu;

@compute(64)
fun bad(out: GpuBuffer<float>): void {
    let x = switch (1) {
        1 => 2.0,
        _ => 3.0,
    };
    out[0] = x;
}
