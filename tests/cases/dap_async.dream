import system;
fun compute(n: int): int {
    let total = 0;
    let i = 0;
    while (i < n) {
        total = total + i;
        i = i + 1;
    }
    return total;
}

async fun main(): void {
    let base = 10;
    let sum = compute(base);
    if (sum > 5) {
        System.println(sum);
    }
}
