// `static` declares class members. Top-level variables are file-private by default, so `static`
// on one carries no meaning and is rejected outright (use `internal`/`public` to widen).
import system;
static let flag: int = 1;

fun main(): void {
    System.println(flag);
}
