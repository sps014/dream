import system;
import system.collections;
import system.text;
fun main(): void {
    let m = Map<string, int>();
    m.set("one", 1);
    m.set("two", 2);
    m.set("three", 3);
    System.println(m.length);
    System.println(m.get("one").unwrap_or(0));
    System.println(m.get("three").unwrap_or(0));
    System.print(m.contains("two"));
    System.print("\n");
    System.print(m.contains("nope"));
    System.print("\n");
    System.println(m.get("missing").unwrap_or(0));
    System.println(m.get_or("missing", -1));
    m.set("two", 22);
    System.println(m.get("two").unwrap_or(0));
    System.println(m.length);
    System.print(m.remove("two"));
    System.print("\n");
    System.println(m.length);
    System.print(m.contains("two"));
    System.print("\n");
    System.println(m.get("two").unwrap_or(0));

    let counts = Map<int, int>();
    let i = 0;
    while i < 40 {
        counts.set(i, i * i);
        i = i + 1;
    }
    System.println(counts.length);
    System.println(counts.get(7).unwrap_or(0));
    System.println(counts.get(39).unwrap_or(0));
    System.print(counts.contains(40));
    System.print("\n");
    System.println(counts.get_or(40, -7));
}
