import system;
import system.gpu;

// Shader arithmetic always wraps (WGSL cannot panic), so `checked` is rejected there.
@gpu
fun helper(x: int): int {
    unchecked {
        let a = x * 3;
    }
    checked {
        return x + 1;
    }
}

fun main(): void {
}
