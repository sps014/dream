// Integer overflow panics inside `checked { }`.
import system;
fun f(a: int): int { let x = a; checked { x += 1; } return x; }
fun main(): void {
    System.println("start");
    System.println(f(2147483647));
    System.println("unreachable");
}
