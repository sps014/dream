// Integer overflow panics inside `checked { }`, even when the operands are constants.
import system;
fun f(): int { let x: int = 2147483647; checked { return x + 1; } }
fun main(): void {
    System.println("start");
    System.println(f());
    System.println("unreachable");
}
