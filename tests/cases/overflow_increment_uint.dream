// Integer overflow panics inside `checked { }`.
import system;
fun f(a: uint): uint { let x = a; checked { x++; } return x; }
fun main(): void {
    System.println("start");
    System.println(f(4294967295u));
    System.println("unreachable");
}
