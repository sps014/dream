// Integer overflow panics by default.
import system;
fun f(a: byte, b: byte): byte { return a - b; }
fun main(): void {
    System.println("start");
    System.println(f(3b, 5b));
    System.println("unreachable");
}
