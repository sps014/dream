// Integer overflow panics by default.
import system;
fun f(a: uint, b: uint): uint { return a - b; }
fun main(): void {
    System.println("start");
    System.println(f(0u, 1u));
    System.println("unreachable");
}
