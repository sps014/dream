// A division by zero must panic even when its result is never used.
import system;
fun main(): void {
    let a = 10;
    let b = 0;
    System.println("before");
    let q = a / b;
    System.println("after");
}
