import system;
import system.task;
async fun main(): void {
    let msg = "hi";
    System.println(Task.spawn_async(async () => {
        Time.sleep(1).await;
        return msg + "!";
    }).await);
}
