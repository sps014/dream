import system;
import system.task;
async fun main(): void {
    let n = 21;
    System.println((Task.spawn_async(async () => {
        Time.sleep(1).await;
        return n * 2;
    }).await).to_string());
}
