// Hard terminate: cancelling the spawn Future aborts a busy worker.
import system;
import system.task;

async fun main(): void {
    let f = Task.spawn(() => {
        while true {
        }
        return 0;
    });
    Promise.cancel(f);
    System.println("done");
}
