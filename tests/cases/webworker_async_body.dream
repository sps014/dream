import system;
async fun tick(): void {
    await Time.sleep(1);
}

async fun work(): string {
    await tick();
    return "hi!";
}

async fun main(): void {
    System.println(await WebWorker.spawn_async(work));
}
