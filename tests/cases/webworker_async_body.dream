import system;
async fun tick(): void {
    Time.sleep(1).await;
}

async fun work(): string {
    tick().await;
    return "hi!";
}

async fun main(): void {
    System.println(WebWorker.spawn_async(work).await);
}
