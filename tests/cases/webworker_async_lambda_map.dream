import system;
async fun main(): void {
    let urls = ["a", "b"];
    let results = await WebWorker.map_async(urls, async (url) => {
        await Time.sleep(1);
        return "resp:" + url;
    });
    for (let r in results) {
        System.println(r);
    }
}
