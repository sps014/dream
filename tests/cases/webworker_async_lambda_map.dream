import system;
async fun main(): void {
    let urls = ["a", "b"];
    let results = WebWorker.map_async(urls, async (url) => {
        Time.sleep(1).await;
        return "resp:" + url;
    }).await;
    for (let r in results) {
        System.println(r);
    }
}
