import system;
async fun fetchLike(url: string): string {
    await Time.sleep(1);
    return "resp:" + url;
}

async fun main(): void {
    let urls = ["a", "b", "c"];
    let results = await WebWorker.map_async(urls, fetchLike);
    for (let r in results) {
        System.println(r);
    }
}
