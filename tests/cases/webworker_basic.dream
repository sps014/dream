// Real parallel WebWorkers. Capture-only spawn; then `.await` the Future.

import system;
fun shout(msg: string): string {
    return msg + "!";
}

fun twice(msg: string): string {
    return msg + msg;
}

async fun main(): void {
    System.println(WebWorker.spawn(() => shout("hello")).await);
    System.println(WebWorker.spawn(() => shout("world")).await);

    let a = WebWorker.spawn(() => shout("A"));
    let b = WebWorker.spawn(() => twice("B"));
    System.println(a.await);
    System.println(b.await);
}
