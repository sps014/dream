// Heap-allocated (non-interned) string captured by a worker: publish must mark TAG_SHARED
// so owner + worker retain/release is atomic.

import system;

async fun main(): void {
    let s = 42.to_string();
    let t = await WebWorker.spawn(() => s + "!");
    System.println(s);
    System.println(t);
}
