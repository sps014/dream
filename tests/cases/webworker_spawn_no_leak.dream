// Finished spawn must join and drop env/funcbox; live_objects must not grow with N.

import system;

fun work(): int {
    return 1;
}

async fun main(): void {
    let _ = WebWorker.spawn(() => work()).await;
    let _ = WebWorker.spawn(() => work()).await;
    let base = Debug.live_objects;
    let i = 0;
    while i < 20 {
        let _ = WebWorker.spawn(() => work()).await;
        i = i + 1;
    }
    System.println((Debug.live_objects - base).to_string());
}
