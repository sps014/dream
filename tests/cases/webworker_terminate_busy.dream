// Hard terminate: cancelling the spawn Future aborts a busy worker.
import system;

async fun main(): void {
    let f = WebWorker.spawn(() => {
        while (true) {
        }
        return 0;
    });
    Promise.cancel(f);
    System.println("done");
}
